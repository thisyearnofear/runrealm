{
  "name": "@runrealm/shared-utils",
  "version": "2.0.0",
  "description": "Shared utilities for RunRealm platform",
  "main": "index.js",
  "scripts": {
    "build": "tsc && echo 'TypeScript compilation completed'",
    "test": "echo 'TODO(M4): enable jest; 3 of 4 specs need fixes (@turf ESM transform, assertion drift)'"
  },
  "dependencies": {
    "@runrealm/shared-types": "^2.0.0"
  }
}
