{
  "name": "@runrealm/shared-utils",
  "version": "2.0.0",
  "description": "Shared utilities for RunRealm platform",
  "main": "index.js",
  "scripts": {
    "build": "(tsc || true) && echo 'TypeScript compilation completed'",
    "test": "echo 'No tests configured for shared-utils'"
  },
  "dependencies": {
    "@runrealm/shared-types": "^2.0.0"
  }
}
